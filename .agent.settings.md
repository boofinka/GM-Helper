preferences:
  allow_runs: true
  allowed_commands:
    - npm
    - gradle
    - git
  code_style:
    javascript: "prettier + eslint"
    kotlin: "ktlint"
  tests: "Suggest and run local builds/tests only with explicit consent"

applyTo:
  - "**/*.js"
  - "**/*.kt"
  - "src_jsons/**"

notes: |
  This file provides project-specific preferences for the CoDev agent. It permits running
  local build commands (npm/gradle) when the user explicitly allows it and recommends
  code style tools to enforce when making changes.
